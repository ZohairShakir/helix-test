name: Helix Test CI

on:
  push:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-lates
    steps:
      - uses: actions/checkout@v3
      
      - name: Run tests
        run: |
          python test_app.py
